name: x
on: pull_request
permissions:
  contents: read
jobs:
  governance:
    name: governance
    runs-on: ubuntu-latest
    steps:
      - run: "true"
  pin-check:
    name: pin-check
    runs-on: ubuntu-latest
    steps:
      - run: "true"
