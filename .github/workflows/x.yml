# SCRATCH — compass-core#52 post-merge step 8 (G3/H5 name-squatting probe). Do not merge.
name: x
on: pull_request
permissions:
  contents: read
jobs:
  governance:
    name: governance
    runs-on: ubuntu-latest
    steps:
      - run: "true"
  pin-check:
    name: pin-check
    runs-on: ubuntu-latest
    steps:
      - run: "true"
